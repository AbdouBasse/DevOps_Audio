🔄 Workflow Audio – Mastering & Mixing
Étapes principales
🎵 Input (Sample brut)

L’utilisateur envoie un fichier audio (sample.wav) via l’API.

Le fichier est stocké temporairement dans data/samples/.

🔍 Analyse & Préprocessing (utils.py)

Vérification du format (WAV/MP3).

Normalisation du volume.

Extraction des features audio (tempo, spectre, dynamique).

🎚️ Mastering (mastering.py)

Application d’un EQ (équilibrage des fréquences).

Compression pour homogénéiser la dynamique.

Normalisation finale du volume.

Export du fichier : sample_mastered.wav.

🎛️ Mixing (mixing.py)

Balance des pistes (gauche/droite).

Spatialisation stéréo (élargissement du champ sonore).

Ajout de reverb pour l’ambiance.

Export du fichier : sample_mixed.wav.

🤖 IA (model.py)

Ajuste automatiquement les paramètres selon le style musical (Afro, Trap, Jazz…).

Exemple : Trap → compression forte, reverb légère.

Retourne les paramètres appliqués.

📤 Output (API Response)

L’API renvoie un JSON avec :

Le nom du fichier traité.

Les paramètres appliqués.

Le statut du traitement.
